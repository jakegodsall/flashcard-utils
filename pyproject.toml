[project]
name = "flashcard-utils"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "jakegodsall", email = "jake.edward.godsall@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.13.5",
    "requests>=2.34.2",
]

[project.scripts]
flashcard-utils = "flashcard_utils:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
